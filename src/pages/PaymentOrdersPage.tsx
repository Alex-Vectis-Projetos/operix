import { PaymentListWorkspace } from "@/components/payment-lists/PaymentListWorkspace";

/** Canonical SPEC-004 commercial List workspace retained at the legacy route. */
export default function PaymentOrdersPage() {
  return <PaymentListWorkspace />;
}
